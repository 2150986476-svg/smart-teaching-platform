{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd 'backend（后端）' && npm install && cd '../frontend（前端）' && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "cd 'backend（后端）' && node src/app.js",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE",
    "numReplicas": 1
  }
}
